pipeline {
    agent any

    stages {
        stage('Build Trigger') {
            steps {
                echo "Code fetched from GitHub successfully"
                echo "Deployment will be handled via Kubernetes"
            }
        }
    }
}